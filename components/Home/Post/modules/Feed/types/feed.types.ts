export type MainProps = {
    images: string[]
}

export type HotProps = {
    topMixtape: string[] 
    topTracks: string[]
    topTrending: string[]
}

export type ReactionProps = {
    textColor: string;
    commentColor: string;
    mirrorColor: string;
    collectColor: string;
    heartColor: string;
}